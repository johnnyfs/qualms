# Godot Prototype

This Godot prototype is unmaintained for now. It was the first 2D orbital-flight proof of concept, and it still uses the older orbital display data in `data/sol_like_system.json` rather than the current story graph representation.

We may return to this interface after the story manager is more settled. For now, treat `../curses/` as the maintained interface.
