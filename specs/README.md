# Specs

This directory contains the technical direction for the Qualms rules engine and future YAML story format.

- `rules-engine.md`: runtime classes, interfaces, action resolution, and deterministic behavior.
- `story-yaml-schema.md`: YAML authoring schema that compiles into the runtime model.
- `runtime-roadmap.md`: implementation roadmap for the YAML-driven runtime, editor, and validation suite.
