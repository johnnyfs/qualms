# Stories

Story data lives here. The current source format is Qualms YAML, with `story_systems.json` kept as a compatibility artifact for the curses editor while migration continues.

Each story directory should contain a `story.qualms.yaml` file. During the transition, it should also contain a generated `story_systems.json` file.
